#                     _
#                    | |_ _ __ ___  _   ___  __
#                    | __| '_ ` _ \| | | \ \/ /
#                    | |_| | | | | | |_| |>  <
#                     \__|_| |_| |_|\__,_/_/\_\                         ver 2.1
#
# written by Shotaro Fujimoto (https://github.com/ssh0)
#------------------------------------------------------------------------------
# General settings                                                          {{{
#------------------------------------------------------------------------------

# enable utf-8
# setw -g utf8 on

# max history limit
set -g history-limit 5000

# escape time
set -s escape-time 0

# Set the time in milliseconds for which the indicators shown by the
# display-pane command appear.
set -g display-panes-time 3000

# count windows stating with 1 instead of 0
set -g base-index 1
set -g pane-base-index 1

# renumber the windows
set -g renumber-windows on

# Automatically set window title
setw -g automatic-rename on

# set-option -g default-command "exec reattach-to-user-namespace -l $SHELL"

# for Yazi's image preview to work correctly in tmux.
set -g allow-passthrough on
set -ga update-environment TERM
set -ga update-environment TERM_PROGRAM

set -g extended-keys on
set -g extended-keys-format csi-u

set -as terminal-features '*:hyperlinks'

#---------------------------------------------------------------------------}}}
# Keybind Documentation                                                     {{{
#------------------------------------------------------------------------------

# I prefered using Alt key instead "Prefix + something".
# And I'm using xmonad, so these key-bindings are similar to xmonad's one.
# My xmonad config is here:
#     https://github.com/ssh0/dotfiles/blob/master/.xmonad/ssh0-home/xmonad.hs
#

# Prefix key: ^g
#
# Abbreviation:
# - [key]
# - (key) <- with Alt (or in MacOS, Command key)
#
# Structure in one session:
#
#         +----------------+
#         |    (←,→,h,l)   ↓(num) OR click status bar       ↓ (Enter)
#      window1 <------> window2          window3         window4
#   ===============================================================
#       pane1            pane1            pane1           pane1
#         ↕ (j,k)
#       pane2     + - -  pane2  - - - -> (pane2)
#                 |               (L)
#      (pane3) <- +      pane3
#                (H)       ↓ (c)                            ↓ (C)
#                          x                                x
#
#
# In a window:
#
#   +---------------+------------+
#   |pane1          |pane3       |
#   |               |            |
#   |               |            |
#   +---------------+            |
#   |pane2          |            |
#   |               |            |
#   |               |            |
#   +---------------+------------+
# 
# split pane:
#   - vertically         : (|)
#   - horizontally       : (-)
# select pane            : (j),(k) OR click the pane with mouse
# shift pane             : (J),(K)
# kill pane              : (c)
# change layout          : (Space)
# fullscreen             : (f)
# resize pane:
#             (a)
#              ↑
#        (,) ← + → (.)
#              ↓        OR drag pane-border with mouse
#             (z)
# join to another window : (H),(L)
# join to another window : [Alt+Shift+num]

#---------------------------------------------------------------------------}}}
# Unbind all keys & default key settings                                    {{{
#------------------------------------------------------------------------------

unbind-key -a

# default key-bindings {{{
#======================#
bind-key C-z suspend-client
bind-key ! break-pane
bind-key '#' list-buffers
bind-key '$' command-prompt "rename-session '%%'"
bind-key , command-prompt "rename-window '%%'"
bind-key - delete-buffer
bind-key . command-prompt "move-window -t '%%'"
bind-key : command-prompt
bind-key \; last-pane
bind-key = choose-buffer
bind-key ? list-keys
bind-key D choose-client
bind-key L switch-client -l
bind-key [ copy-mode
bind-key ] paste-buffer
bind-key d detach-client
bind-key f command-prompt "find-window '%%'"
bind-key i display-message
bind-key q display-panes
bind-key s choose-session
bind-key w choose-window
bind-key '~' show-messages
bind-key PPage copy-mode -u
bind-key -r M-1 select-layout even-horizontal
bind-key -r M-2 select-layout even-vertical
bind-key -r M-3 select-layout main-horizontal
bind-key -r M-4 select-layout main-vertical
bind-key -r M-5 select-layout tiled
# }}}

#---------------------------------------------------------------------------}}}
# Keybind (normal mode)                                                     {{{
#------------------------------------------------------------------------------

# prefix key ^b -> ^g
set -g prefix C-g
bind-key C-g send-prefix

# vi like key binding
setw -g mode-keys vi
bind-key -T copy-mode-vi v send-keys -X begin-selection

# if session has > 1 windows in current, kill-pane without confirmation.
# But confirm before killing pane when it is the last pane in the window.
bind-key -n M-c if "tmux display -p \"#{window_panes}\" | grep ^1\$" \
    "confirm-before -p \"Kill the only pane in the window? It will kill this window too. (y/N)\" kill-pane" \
    "kill-pane"

# if session has > 1 windows in current, kill-window without confirmation.
# But confirm before killing window when it is the last window in the session. 
bind-key -n M-C if "tmux display -p \"#{session_windows}\" | grep ^1\$"\
    "confirm-before -p \"Kill the only window in this session? It will kill the session too. (y/N)\" kill-window" \
    "kill-window"

# split window (easily remember)
bind-key -n M-- split-window -v -c "#{pane_current_path}"
bind-key -n M-| split-window -h -c "#{pane_current_path}"

# vim like
bind-key s split-window -v -c "#{pane_current_path}"
bind-key v split-window -h -c "#{pane_current_path}"

# move between panes
bind-key -n M-j select-pane -D
bind-key -n M-k select-pane -U
bind-key -n M-h select-pane -L
bind-key -n M-l select-pane -R

bind-key -n M-Down select-pane -t :.+
bind-key -n M-Up select-pane -t :.-

# select the master pane
bind-key -n M-m select-pane -t :.1

# swap pane
bind-key -n M-J swap-pane -D
bind-key -n M-K swap-pane -U
bind-key -n M-S-Down swap-pane -D
bind-key -n M-S-Up swap-pane -U

# move the pane to the master pane
bind-key -n M-M swap-pane -d -t :.1

# swap pane to Alt+shift+number
bind-key -n M-! join-pane -t :1
bind-key -n M-'"' join-pane -t :2
bind-key -n M-'#' join-pane -t :3
bind-key -n M-'$' join-pane -t :4
bind-key -n M-% join-pane -t :5
bind-key -n M-& join-pane -t :6
bind-key -n M-"'" join-pane -t :7
bind-key -n M-( join-pane -t :8
bind-key -n M-) join-pane -t :9

# resize panes (xmonad like)
bind-key -n M-',' resize-pane -L 1
bind-key -n M-z resize-pane -D 1
bind-key -n M-a resize-pane -U 1
bind-key -n M-'.' resize-pane -R 1

# maximize pane ("f"ull screen)
bind-key -n M-f resize-pane -Z

# display-panes
bind-key -n M-q display-panes

# move between window
bind-key -n M-Right next-window
bind-key -n M-Left previous-window

# swap window
bind-key -n M-S-Right swap-window -t :+
bind-key -n M-S-Left swap-window -t :-

# next layout
bind-key -n M-Space next-layout

# switch windows Alt+number
bind-key -n M-1 select-window -t 1
bind-key -n M-2 select-window -t 2
bind-key -n M-3 select-window -t 3
bind-key -n M-4 select-window -t 4
bind-key -n M-5 select-window -t 5
bind-key -n M-6 select-window -t 6
bind-key -n M-7 select-window -t 7
bind-key -n M-8 select-window -t 8
bind-key -n M-9 select-window -t 9

# select-window by name typing
bind-key W command-prompt "select-window -t '%%'"

# create new window
bind-key -n M-Enter new-window -c "#{pane_current_path}"

# choose-tree
bind-key t choose-tree -w -Z -F "#{?pane_format,#[fg=colour229]#{pane_current_command} #[fg=colour247]#{pane_title},#{?window_format,#[fg=colour157]#{window_name}#{window_flags}#{?#{==:#{window_panes},1}, #{?#{!=:#{window_name},#{pane_current_command}},#[fg=colour11]#{pane_current_command} ,}#[fg=colour247]#{pane_title},},#[fg=colour238,bg=colour51]#{?session_grouped, (group #{session_group}: #{session_group_list}),}#{?session_attached, attached ,#[fg=colour238,bg=colour124] unattached }}}"

# Capture pane and open in Vim
bind-key C-c run 'tmux capture-pane -S -10240 -p > /tmp/tmux-capture.txt'\;\
             new-window "vim + /tmp/tmux-capture.txt"

# Toggle status bar
bind-key -n M-0 set status

# ^b + r => reload config file
bind-key r source-file ~/.tmux.conf \; display "Reloaded!"

#---------------------------------------------------------------------------}}}
# Keybind (copy mode)                                                       {{{
#------------------------------------------------------------------------------

# v: select start with v (default Space)
bind-key -T copy-mode-vi v     send-keys -X begin-selection

# V: select line
# bind-key -T vi-copy V select-line

# C-v: select rectangular
# bind-key -T vi-copy C-v rectangle-toggle

# y: yank
bind-key -T copy-mode-vi y send-keys -X copy-pipe-and-cancel "reattach-to-user-namespace pbcopy"

# Y: yank line
# bind-key -T vi-copy Y copy-line

bind-key -T copy-mode-vi Enter send-keys -X copy-pipe-and-cancel "reattach-to-user-namespace pbcopy"


#---------------------------------------------------------------------------}}}
# Mouse settings                                                            {{{
#------------------------------------------------------------------------------

set -g mouse on

#---------------------------------------------------------------------------}}}
# Color                                                                     {{{
#------------------------------------------------------------------------------

# use 256colors
set -g default-terminal screen-256color


# # pane border
# set -g pane-active-border-style fg=colour197,bg=colour235
# set -g pane-border-style fg=colour239,bg=colour235

# # window list
# setw -g window-status-style fg=white,bg=colour235

# # message line
# # set -g message-fg white
# # set -g message-bg colour197
# # set -g message-attr bright

# set -g message-command-style bg=colour255,fg=colour235
# set -g message-style bg=colour235,fg=colour255
# set -g mode-style bg=colour197,fg=colour235

#====================

# Name: Flexoki
# Variant: Light
# URL: https://stephango.com/flexoki
# Description: An inky color scheme for prose and code.
# Note: Color hexes in lower case to avoid tmux flag confusion

flexoki_black="#100f0f"
flexoki_base_950="#1c1b1a"
flexoki_base_900="#282726"
flexoki_base_850="#343331"
flexoki_base_800="#403e3c"
flexoki_base_700="#575653"
flexoki_base_600="#6f6e69"
flexoki_base_500="#878580"
flexoki_base_300="#b7b5ac"
flexoki_base_200="#cecdc3"
flexoki_base_150="#dad8ce"
flexoki_base_100="#e6e4d9"
flexoki_base_50="#f2f0e5"
flexoki_paper="#fffcf0"

flexoki_red="#d14d41"
flexoki_orange="#da702c"
flexoki_yellow="#d0a215"
flexoki_green="#879a39"
flexoki_cyan="#3aa99f"
flexoki_blue="#4385be"
flexoki_purple="#8b7ec8"
flexoki_magenta="#ce5d97"

flexoki_red_2="#af3029"
flexoki_orange_2="#bc5215"
flexoki_yellow_2="#ad8301"
flexoki_green_2="#66800b"
flexoki_cyan_2="#24837b"
flexoki_blue_2="#205ea6"
flexoki_purple_2="#5e409d"
flexoki_magenta_2="#a02f6f"

color_tx_1=$flexoki_black
color_tx_2=$flexoki_base_600
color_tx_3=$flexoki_base_300
color_bg_1=$flexoki_base_200
color_bg_2=$flexoki_base_100
color_ui_1=$flexoki_base_200
color_ui_2=$flexoki_base_300
color_ui_3=$flexoki_base_500

color_red=$flexoki_red
color_orange=$flexoki_orange
color_yellow=$flexoki_yellow
color_green=$flexoki_green
color_cyan=$flexoki_cyan
color_blue=$flexoki_blue
color_purple=$flexoki_purple
color_magenta=$flexoki_magenta

# status
set -g status-position top
set -g status on
set -g status-bg $color_bg_2
set -g status-justify "left"
set -g status-left-length "100"
set -g status-right-length "100"

# messages
set -g message-style "fg=$color_tx_1,bg=$color_bg_2,align=centre"
set -g message-command-style "fg=$color_tx_1,bg=$color_ui_2,align=centre"

# panes
set -g pane-border-style fg=$color_ui_2
set -g pane-active-border-style fg=$color_blue

# windows
setw -g window-status-activity-style fg=$color_tx_1,bg=$color_bg_1,none
setw -g window-status-separator " "
setw -g window-status-style fg=$color_tx_1,bg=$color_bg_1,none

# statusline
set -g status-left "#{?client_prefix,#[fg=#$flexoki_paper#,bg=#$color_red],#[fg=#$flexoki_paper#,bg=#$color_green]}  #S #{?client_prefix,#[fg=#$color_red#,bg=#$color_bg_2],#[fg=#$color_green#,bg=#$color_bg_2]}#[fg=#$color_bg_2#,bg=#$color_bg_2]  "
set -g status-right "#[fg=#$color_paper,bg=#$color_orange]  #(echo #{pane_current_path} | sed 's#$HOME#~#g') "


# window-status
setw -g window-status-format "#[bg=#$color_bg_2,fg=#$color_tx_2]  #I · #W  "
setw -g window-status-current-format "#[bg=#$color_bg_2,fg=#$color_bg_1]#[bg=#$color_bg_1,fg=#$color_tx_1] #I ● #W #[bg=#$color_bg_2,fg=#$color_bg_1]"


# set -g status-left "#{?client_prefix,#[fg=#$flexoki_paper,bg=#$color_red],#[fg=#$flexoki_paper,bg=#$color_green]} ╭  #S #{?client_prefix,#[fg=#$color_red,bg=#$color_bg_2],#[fg=#$color_green,bg=#$color_bg_2]}╯#[fg=#$color_bg_2,bg=#$color_bg_2]  "
# set -g status-right "#[fg=#$color_paper,bg=#$color_orange] ╭ 󰉋 #(echo #{pane_current_path} | sed 's#$HOME#~#g') ╯ "


# Modes
setw -g clock-mode-colour $color_blue
setw -g mode-style fg=$color_orange,bg=$color_tx_1,bold



#---------------------------------------------------------------------------}}}
# WM window title                                                           {{{
#------------------------------------------------------------------------------

# enable wm window titles
set -g set-titles on

# wm window title string (uses status bar variables)
set -g set-titles-string "#S - #W [#I/#{session_windows}] (#P/#{window_panes})"

#---------------------------------------------------------------------------}}}
# Status line                                                               {{{
#------------------------------------------------------------------------------

# source $HOME/.config/tmux/status_powerline.conf
# source $HOME/.config/tmux/status.conf

#---------------------------------------------------------------------------}}}
